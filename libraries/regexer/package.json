{
  "name": "regexer",
  "version": "0.0.1",
  "description": "Core engine executing regexes step by step.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json && tsc-alias -p tsconfig.json && peggy ./src/models/regex.pegjs --format es --output ./dist/models/regex.js",
    "start": "node dist/index.js",
    "dev": "node --trace-warnings dist/index.js",
    "test": "jest index"
  },
  "author": "Dominik Kundra, KUN0161",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.7",
    "jest": "^29.7.0",
    "peggy": "^3.0.2",
    "ts-jest": "^29.1.1",
    "tsc-alias": "^1.8.8"
  }
}
