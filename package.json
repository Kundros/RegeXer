{
  "name": "regex-visualisation-thesis",
  "version": "0.0.1",
  "description": "This package is source package for thesis on regex visualisation plugin for vs-code.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@kundros/regex-visualisation": "file:./libraries/regex-visualisation/dist",
    "@kundros/regexer": "file:./libraries/regexer/dist"
  },
  "author": "Dominik Kundra, KUN0161",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
